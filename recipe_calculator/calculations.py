def calculate_evergy_value(ingridients):
    #рассчёт
    return
sum(ingridient['energy'] for ingridient in ingridients)

def calculate_cost:
    #рассчёт
    return
sum(ingtidient['cost'] for ingridient in ingridients)
    
